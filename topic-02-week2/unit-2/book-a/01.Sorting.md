# 01. sort, cut

sort · cut · | pipe

+ Use **sort** to return back an alphabetically sorted list of the employee's names.

Store this list in a new file called **EmpByName.txt**

## The **cut** command

+ Using **cut**, write the code to only return the first three characters (i.e. the employee ID) from the **employee.txt** file

+ Using **cut**, write the code to only return the employee's names from the **employee.txt** file

TIP: second column from the single space delimited file


## The **|** pipe command 


Consider the outcome of the commands below:

```bash
$ls | head -3 | tail -1 > myoutput
```
and 

```bash
$ls ~ | wc –l
$cat ~/employee.txt | wc –l
```

+ Recreate a new **numEmp** file that returns the below basic details from **employee.txt** when executed (with the correct number of employees as per your own file):

```
$./num*
Today on Wednesday 28 Jan 2026 14:06:09 IST there are 0 employees.
```