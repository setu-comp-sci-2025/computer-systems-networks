## Run as Systemd services + deploy via GitHub

For a more production-like setup on your Pi 4:

1. Keep this code in your GitHub repo (`smart-doorbell`).
2. Create a systemd service:
   - `doorbell-edge.service` running `doorbell_camera.py`
3. Add a `deploy.sh` + systemd **timer** to periodically `git pull` and restart on new commits.

This lets you update the project simply by:

- Editing code locally or on another machine,
- Committing and pushing to GitHub,
- Letting the Pi automatically pull and restart.

---

## 11. Checkpoint / Reflection

By now you should be able to:

+ Initialise a **Git repository** on the Pi and push your project to **GitHub**

- Use **Picamera2** on Raspberry Pi OS (Trixie) to capture still images from the Camera Module 3.
- Use the **Sense HAT joystick** on a Pi 4 as a “doorbell button”.
- Store simple **state** event as JSON and use it from multiple components.
- Build a small **Flask** web app that serves a live dashboard from local files.

> **Extension ideas:**  
>
> - xxxxxxxxxx git add .git commit -m "Describe your change"git pushbash
> - Add a second page showing a time-series chart of temperature/humidity.  
> - Style the web page with a CSS framework.  
> - Add a “door open” command via MQTT that changes the Sense HAT colours.  
> - Set up automatic de