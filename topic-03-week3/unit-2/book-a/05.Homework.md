# 5% Homework

HOMEWORK: Filtering your script

# Solution to Print only the last column of the `/etc/shells` file

```bash
$awk -F "/" '/^\// {print $NF}'
```

-  pipe this whole command into the `uniq` command to ensure duplicates are removed 
- pipe this unique list into the `sort` command

# 5% HOMEWORK

1. With the aid of `grep` and `awk`, create a script that will ask the user to choose any persons name from your **employee.txt** file. 
2. The system is to return the name of said employee along with their associated expenses (i.e. the value in the last column of that txt file)

When writing your script, consider:

 + the user may need an option to **add** a new user to the system
 
 + the user may need an option to **view** the file to see the names in the system before looking at their expenses? 

+ search terms should be **case insensitive** 

+ the user should be able to exit the script at any time?

+ the system should be end-user friendly i.e. simple yet easy to read and understand the options presented and their outputs

+ Include your name as **Author:** on the script as well as a brief **Description:** of your script

Are there any other error handling or other considerations you would like to implement?

*This screenshot might be similar to a FIRST DRAFT output of your script* 

![Screenshot of what an draft output might look like:](./img/filter.png)

Please upload to [Moodle](https://moodle.setu.ie/mod/assign/view.php?id=4395810)

(1) your code (this is to be a simple text file) plus

(2)  a brief video (2 minutes'ish) of your working script

    Demo your code on one half of the screen and the code execution on the other half of the screen, similar to:

![Demo Sample](./img/demoScreen.png)

Using any screen recording software:

i. demo the menu options presented to the user 

ii. show the records in your system 
(maybe a `view records` option?)

iii. show new record fields being populated and stored in the system (maybe through a `Add New Record` menu option?)

iv. again, show the records in your system where the new record should now be also present (maybe re-use `view records` option?)

v. demo system returning the expenses of record as selected by user via suitably named menu option

vi. demo exiting the system via suitably named option

## CA Due Date:

- Due the end of the first reading week 

- Value: 5% of your CA

**GRADING RUBRIC**

- 0.5% User experience (easy to understand & use)
- 3% completed working code
- 0.5% good coding layout (expected shebang, author, desc lines; indentation, comments)
- 1% Error handling


