# Subnet Mask

A Subnet mask is a 32-bit number, combined with an with IPv4 address, used to differentiate the network portion of the IP address from the host portion. It is a crucial component in IP network design and routing. 



### Subnet Mask Conversion

Use the content on the module web site  to answer the following:

1. Convert the dot-decimal subnet mask `255.255.128.0` into its binary form
2. Convert the dot-decimal subnet mask `255.255.255.192` into its binary form
3. Explain why **255.255.251.0** is **not** a valid Subnet Mask.(hint: convert it to binary and have a look at it)

 Check your answer with a suitable online tool.

### Network/Broadcast/Host Addresses

1. Calculate the **first** and **last** useable host address on the network **172.26.160.0**, subnet mask **255.255.255.192**
2. Calculate the **last useable host address** and **Broadcast address** for the ip address **172.27.16.64/22**?
