# Movement Event 

Events are used to track, log, and work with important events. In this section, you'll define an event to track unexpected movements of the movement detection device.

+ In the Blynk Console, do to Developer Zone -> My Templates
+ Select the **Movement Detection Template** and open the "**Events & Notifications**" tab.
  <img src="./img/02.Create Event/image-20241202170045072.png" alt="image-20241202170045072" style="zoom:50%;" />

- Click **+ Create Event**
- Name the first event with the name Hello
- Click **Create**  and, in the general tab, enter the following values:
   <img src="./img/02.Create Event/image-20241202170316532.png" alt="image-20241202170316532" style="zoom:67%;" />
- Open the Settings tab and enter the following:
  <img src="./img/02.Create Event/image-20241202170411734.png" alt="image-20241202170411734" style="zoom:50%;" />
- Click **Create** followed by  **Save and Apply**. You should see the event in the Events and Notifications list. 

<img src="./img/02.Create Event/image-20241202170539360.png" alt="image-20241202170539360" style="zoom:50%;" />

## Create Event from Movement Detection Device

Now that you have defined an event, lets update the code on the Rapsberry Pi to create an event every time the RPi is Shaken. 

+ If not already open, open the ``blynk`_movement.py`` script you created in the last step.

+ Add the following line of code just before the statement the turns the LEDs red(``sense.clear(RED)``)

  ```python
  blynk.log_event("movement_event") # Has to match name of event in Blynk, including case!!!
  ```

+ Run the program again(or stop/start it if already running)

+ Shake the RPi to generate an event.

+ Now go back to the Device in the BLynk Console and select the Bell icon to open the Notifications and Events history. You should see the movement event created by the shake.

  <img src="./img/02.Create Event/image-20241202171427517.png" alt="image-20241202171427517" style="zoom:50%;" /> 